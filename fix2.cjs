const fs = require('fs'); let code = fs.readFileSync('src/pages/Admin.tsx', 'utf8'); const idx = code.indexOf('<option value=\"hidden\">'); if (idx > -1) { const onClickIdx = code.indexOf('onClick={async (e) => {', idx); if (onClickIdx > -1) { const replace = \<option value=\"hidden\">إخفاء الراديو من الشات</option>\n                          </select>\n                          <p className=\"text-[11px] text-gray-400 mt-1\">ℹ️ يمكنك تغيير مكان الراديو.</p>\n                        </div>\n\n                        <div className=\"bg-red-50 dark:bg-red-900/10 p-6 rounded-2xl border-2 border-red-500/50 flex items-center justify-between\">\n                          <div>\n                            <h4 className=\"font-black text-lg text-red-700 dark:text-red-400 flex items-center gap-2\">\n                              <ShieldAlert className=\"w-5 h-5\" /> ريستارت للشات\n                            </h4>\n                            <p className=\"text-xs text-red-600/70 mt-1\">سوف يقوم بحذف رسائل الجميع وتحديث الصفحة.</p>\n                          </div>\n                          <button \n                            \; code = code.substring(0, idx) + replace + code.substring(onClickIdx); fs.writeFileSync('src/pages/Admin.tsx', code, 'utf8'); console.log('Fixed'); } else { console.log('no onClickIdx'); } } else { console.log('no idx'); }
